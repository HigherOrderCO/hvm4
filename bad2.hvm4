@c2   = λs. !S0&C=s; λx0.S0₀(S0₁(x0))
@k2   = λs. !S0&K=s; λx0.S0₀(S0₁(x0))
@main = @c2(@k2)
//
//λa.λb.A₀
//! A &K = B₀(C₁);
//! B &K = a;
//! C &C = @{A₁,D₁,b}B₁(c);
//! D &K = A₁;
//
//
//λa.λb.A₀
//! A &K = B₀(C₁);
//! B &K = a;
//! C &C = @{A₁,D₁,b}B₁(c);
//! D &K = A₁;

//λa.λb.A₀
//! A &K = B₀(C₁);
//! B &K = a;
//! C &C = D₁(b);
//! D &K = A₁;

//λa.λb.A₀
//! A &K = a(D₁(b));
//! D &K = A₁;
